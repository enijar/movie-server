{
  "name": "movie-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "postinstall": "concurrently -n app 'npm --prefix app install'",
    "start": "concurrently -n app 'npm --prefix app run dev'",
    "build": "concurrently -n app 'npm --prefix app run build'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.0.3"
  },
  "engines": {
    "node": ">=v18.17.0"
  }
}
